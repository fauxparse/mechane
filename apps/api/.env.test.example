# Dedicated database on the local Postgres server for database-backed tests.
# The postgres-init service in docker-compose.yml creates this database.
DATABASE_URL=postgres://mechane:mechane@localhost:5433/mechane_test
BETTER_AUTH_SECRET=test-only-secret
BETTER_AUTH_URL=http://localhost:4000
