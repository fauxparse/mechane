// Shared Command/undo-redo engine used by both the Canvas and Flow editors
// in studio. Undo is implemented by sending the inverse of a change as
// an ordinary forward command — see docs/adr/0005-undo-as-forward-commands.md.
export {};
