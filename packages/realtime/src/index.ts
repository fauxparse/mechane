// Internal pub/sub abstraction (RealtimeChannel.publish/subscribe) over the
// realtime provider. Application code must depend on this interface, never
// the provider SDK directly — see docs/adr/0003-ably-behind-realtime-abstraction.md.
export {};
