// Theme tokens (light/dark, slate-pink default, gruvbox alt) and shared
// component primitives (Tailwind + BaseUI + shadcn/ui). Every new visual
// component added here needs a Storybook story in the same change — see
// PRD.md §9 "Component convention".
export {};
